[package]
name = "rsflow-nodes"
version = "0.1.0"
edition = "2024"

[dependencies]
rsflow-core = { path = "../rsflow-runtime/rsflow-core" }
flow-node-inject = { path = "./inject" }
flow-node-log = { path = "./log" }
flow-node-shell = { path = "./shell" }
async-trait = "0.1"
